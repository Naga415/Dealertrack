// Exposing fusion-core functionality via Fusion to avoid
// a breaking change and to keep imports in existing apps simple
// Can consider a breaking release where fusion-starter's /src/api/api.js
// imports from fusion-core instead of a fusion to avoid this.
var fusionCore = require('@coxautokc/fusion-core');
module.exports = {
    baseUrl: fusionCore.baseUrl
};