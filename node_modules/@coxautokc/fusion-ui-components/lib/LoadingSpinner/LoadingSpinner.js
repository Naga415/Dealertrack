'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});

var _react = require('react');

var _react2 = _interopRequireDefault(_react);

var _propTypes = require('prop-types');

var _propTypes2 = _interopRequireDefault(_propTypes);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

/** Preloader icon. */
var LoadingSpinner = function LoadingSpinner(props) {
  var htmlId = props.htmlId,
      className = props.className,
      floatToSide = props.floatToSide,
      size = props.size;


  var mergedClassesContainer = floatToSide ? ' ' + className + ' loading-spinner--' + floatToSide : className;
  var mergedClassesOuter = size ? ' loading-spinner--' + size : '';

  return _react2.default.createElement(
    'div',
    { id: htmlId, className: 'loading-spinner' + mergedClassesContainer },
    _react2.default.createElement(
      'span',
      { className: 'loading-spinner__outer' + mergedClassesOuter },
      _react2.default.createElement(
        'span',
        { className: 'loading-spinner__inner' },
        'Loading\u2026'
      )
    )
  );
};

LoadingSpinner.propTypes = {
  /** ClassName for spinner. */
  className: _propTypes2.default.string,

  /** Float property ( left/right ). */
  floatToSide: _propTypes2.default.oneOf(['left', 'right']),

  /** Globally unique and descriptive HTML ID. Used by QA for automated testing. */
  htmlId: _propTypes2.default.string.isRequired,

  /** Size of spinner ( small/large ). */
  size: _propTypes2.default.oneOf(['small', 'large'])
};

LoadingSpinner.defaultProps = {
  className: ''
};

exports.default = LoadingSpinner;